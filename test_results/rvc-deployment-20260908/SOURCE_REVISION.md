# Deployed source revision

The verified RVC firmware and matched Pi runtime are recorded in local Git commit
`65bb4317f9ba1c2fceaea98502a96ad5d7457366` on `feat/imu-navigation-controls`.
Before committing, all 54 non-Markdown files in `release-manifest.json` matched
their deployed package SHA-256 hashes. Documentation was subsequently updated
to describe deployment. Git may normalize text line endings; the release manifest
retains the exact packaged-byte hashes. No push is claimed.

Notion task 01 source-revision update succeeded after transient fetch transport
errors. Hardware details and Nathan's baseline confirmation remain
required to close task 01; source traceability alone does not complete it.
